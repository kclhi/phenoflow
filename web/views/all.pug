extends layout

block content

  div(class="container")
    p(style='white-space:pre;')
      each workflow in workflows
        a(href="download/" + workflow.id) #{workflow.name}
        |  #{workflow.about}
