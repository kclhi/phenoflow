extends layout

block header
  | Library

block content
  div(class="container")
    p(style='white-space:pre;')
      each workflow in workflows
        div(class="row" style='white-space:pre;')
          p(style='white-space:pre;')
            a(href="/phenoflow/phenotype/download/" + workflow.id class="a-button") #{workflow.name}
            |  #{workflow.about}
